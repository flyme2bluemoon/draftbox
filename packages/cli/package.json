{
  "name": "draftbox",
  "version": "0.1.0",
  "type": "module",
  "description": "Upload, version, and share single-file HTML pages.",
  "license": "MIT",
  "homepage": "https://github.com/flyme2bluemoon/draftbox",
  "bugs": "https://github.com/flyme2bluemoon/draftbox/issues",
  "engines": {
    "node": ">=22.11"
  },
  "bin": {
    "draftbox": "dist/index.js"
  },
  "files": [
    "README.md",
    "dist/index.js",
    "dist/LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/flyme2bluemoon/draftbox.git",
    "directory": "packages/cli"
  },
  "scripts": {
    "build": "node ./build.mjs",
    "check": "tsc --noEmit",
    "cli": "node dist/index.js",
    "dev": "DRAFTBOX_API_URL=\"http://localhost:8787\" pnpm run cli",
    "prepack": "pnpm run build",
    "test": "tsc && node --test --experimental-strip-types test/**/*.test.ts"
  },
  "dependencies": {
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@draftbox/contracts": "workspace:*",
    "esbuild": "^0.25.12"
  }
}
