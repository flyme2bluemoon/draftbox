{
    "name": "@draftbox/contracts",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "Shared runtime schemas and TypeScript types for the Draftbox API.",
    "exports": {
        ".": {
            "types": "./src/index.ts",
            "default": "./dist/index.js"
        }
    },
    "files": [
        "dist",
        "src"
    ],
    "scripts": {
        "build": "tsc",
        "check": "tsc --noEmit",
        "test": "pnpm run build && node --test --experimental-strip-types test/**/*.test.ts"
    },
    "dependencies": {
        "zod": "^4.4.3"
    }
}
